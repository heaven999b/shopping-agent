"""Global constants for SHOP-PLAN Shopping Agent."""

# ---------------------------------------------------------------------------
# 模型配置
# ---------------------------------------------------------------------------
DEFAULT_MODEL = "claude-opus-4-6"
DEFAULT_MAX_TOKENS = 16000

# ---------------------------------------------------------------------------
# 澄清策略阈值
# ---------------------------------------------------------------------------
CLARIFICATION_UNCERTAINTY_THRESHOLD = 0.3   # 低于此值停止澄清
CLARIFICATION_MAX_ROUNDS = 2                 # 最多问 2 轮
CLARIFICATION_MAX_OPTIONS = 3               # 选项题最多 3 个选项
MIN_INFO_GAIN_TO_ASK = 0.2                  # 信息增益低于此值不问

# ---------------------------------------------------------------------------
# 候选商品限制
# ---------------------------------------------------------------------------
MAX_CANDIDATES_PER_SLOT = 20    # 每个坑位最多保留的候选数
MAX_PLANS_OUTPUT = 3            # 最多输出 3 套方案
RETRIEVAL_TOP_K = 50            # 初始召回数量

# ---------------------------------------------------------------------------
# 数据时效（秒）
# ---------------------------------------------------------------------------
PRODUCT_DATA_TTL = 300          # 商品价格/库存 5 分钟 TTL
USER_PROFILE_TTL = 3600         # 用户画像 1 小时缓存
PLAN_TTL = 600                  # 方案有效期 10 分钟

# ---------------------------------------------------------------------------
# 风险控制
# ---------------------------------------------------------------------------
HIGH_RISK_AMOUNT_THRESHOLD = 2000.0   # 单次下单金额超过此值需要确认（元）
MAX_TOOL_RETRIES = 2                  # 工具调用最大重试次数
TOOL_RETRY_BASE_DELAY = 1.0           # 指数退避基础延迟（秒）

# ---------------------------------------------------------------------------
# 评分权重
# ---------------------------------------------------------------------------
SCORE_WEIGHT_CONSTRAINT = 0.5    # 约束满足度权重
SCORE_WEIGHT_PREFERENCE = 0.3    # 偏好匹配度权重
SCORE_WEIGHT_VALUE = 0.2         # 性价比权重

# ---------------------------------------------------------------------------
# 平台标识
# ---------------------------------------------------------------------------
SUPPORTED_PLATFORMS = ["JD", "Tmall", "PDD", "Taobao", "Suning"]

# ---------------------------------------------------------------------------
# 常见品类标签
# ---------------------------------------------------------------------------
BUNDLE_CATEGORIES = {
    "pc_setup": ["cpu", "motherboard", "ram", "gpu", "storage", "case", "psu", "cooling"],
    "desk_setup": ["monitor", "keyboard", "mouse", "desk", "chair", "webcam", "headset"],
    "business_outfit": ["shirt", "pants", "jacket", "shoes", "belt", "bag"],
    "camping_gear": ["tent", "sleeping_bag", "backpack", "headlamp", "stove", "water_filter"],
}
